{
  "name": "cdn-node-operator-core-v1",
  "version": "0.3.0-phase6",
  "description": "3x-ui CDN Operator Core-v1 source candidate with one local stdio MCP server and Core Hooks. Phases 0-6 source only; real infrastructure is not claimed.",
  "license": "UNLICENSED",
  "files": [
    ".codex-plugin/",
    ".mcp.json",
    "DEPENDENCIES.json",
    "README.md",
    "contract/",
    "hooks/",
    "lifecycle/",
    "mcp/",
    "runtime/",
    "skills/"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "extract-contract": "node scripts/extract-contract.cjs",
    "test": "node --test tests/*.test.cjs",
    "doctor": "node lifecycle/doctor.cjs",
    "acceptance": "node scripts/acceptance.cjs",
    "package:check": "node --test tests/package-release.test.cjs"
  },
  "dependencies": {
    "ajv": "8.20.0"
  },
  "bundleDependencies": [
    "ajv"
  ]
}
